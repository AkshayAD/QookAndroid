# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment Variables - CRITICAL SECURITY
.env
.env.local
.env.*.local
.env.development
.env.production
android/keystore.properties
android/app/upload-keystore.jks
release_builds/
analysis/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Untracked debug captures in artifacts/
artifacts/manual-demo-*
artifacts/manual-emulator-*
artifacts/manual-final-*
artifacts/emulator-*
artifacts/login-after-*
artifacts/cold-launch-*
artifacts/phone-test-apk/
artifacts/play-release/
artifacts/qa-*/
artifacts/*.xml
artifacts/*.png
artifacts/*.jpg
artifacts/*.txt
!artifacts/payment-audit-*/

# Lockfile for unused package manager
pnpm-lock.yaml

# Optional scratch dir for local captures
.local/

# Gradle wrapper cache (machine-local)
.gradle-user/

# Workspace-level planning documents
CLEANUP_PLAN.md

# Marketing assets — untracked 2026-05-23; blobs remain in past history
# (run git filter-repo separately if history rewrite is ever needed)
QookCommander Marketing/
